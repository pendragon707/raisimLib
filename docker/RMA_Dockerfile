FROM ros:noetic

RUN apt-get update && \
    apt-get install -y wget mesa-utils && \
    sudo apt-get install libeigen3-dev && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Install Miniconda
RUN wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O /root/miniconda.sh && \
    mkdir -p /root/.conda && \
    bash /root/miniconda.sh -b -p /root/miniconda3 && \
    rm -f miniconda.sh

ENV PATH=/root/miniconda3/bin:$PATH

ENV PATH=/root/miniconda3/bin:$PATH
RUN conda tos accept --channel https://repo.anaconda.com/pkgs/main && \
    conda tos accept --channel https://repo.anaconda.com/pkgs/r

RUN conda create -n rma python=3.8

ARG PYTORCH_CUDA_VERSION=12.1
ENV PYTORCH_CUDA_VERSION=${PYTORCH_CUDA_VERSION}

ENV CONDA_DEFAULT_ENV=rma
ENV CONDA_PREFIX=/root/miniconda3/envs/rma
ENV PATH=/root/miniconda3/envs/rma/bin:/root/miniconda3/bin:$PATH

# Source conda.sh to enable `conda activate`
RUN . /root/miniconda3/etc/profile.d/conda.sh && \
      conda activate rma

WORKDIR /workspace

COPY docker/rma_entry.sh .

# RUN ["chmod", "+x", "/workspace/rma_entry.sh"]
# RUN "/workspace/rma_entry.sh"