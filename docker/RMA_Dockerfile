FROM ros:noetic

RUN apt-get update && \
    apt-get install -y wget mesa-utils && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Install Miniconda
RUN wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O /root/miniconda.sh && \
    mkdir -p /root/.conda && \
    bash /root/miniconda.sh -b -p /root/miniconda3 && \
    rm -f miniconda.sh

ENV PATH=/root/miniconda3/bin:$PATH

WORKDIR /home

COPY ./rma.yml .
RUN conda env create -n rma --file /home/rma.yml

COPY rma_entry.sh .
COPY . raisimLib